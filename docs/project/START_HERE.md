# Start Here: AirBattery

این فایل نقطه شروع اجباری هر چت جدید یا ادامه توسعه است.

## ترتیب مطالعه

1. `docs/project/AIRBATTERY_MASTER_ROADMAP.md`
2. جدیدترین فایل داخل `docs/project/progress/`
3. Design و Plan فاز فعال
4. وضعیت Git

```bash
cd ~/projects/personal/AirBattery
git status -sb
git log -5 --oneline --decorate
```

## قوانین قطعی

- تسک بعدی از روی آخرین باگ دیده‌شده انتخاب نمی‌شود.
- هر قابلیت باید جای مشخصی در Roadmap داشته باشد.
- داده Left/Right/Case فقط وقتی نمایش داده می‌شود که واقعاً دریافت شده باشد.
- مقدار Aggregate به اجزای ساختگی تبدیل نمی‌شود.
- همه دستگاه‌های Active در داشبورد اصلی دیده می‌شوند.
- فایل‌های Progress قبلی بازنویسی نمی‌شوند؛ هر بار فایل جدید ساخته می‌شود.
- درصد کلی پروژه تا تکمیل ماتریس قابلیت‌ها `Unscored` باقی می‌ماند.

## وضعیت پایه

- Stable branch: `main`
- Stable commit: `3530385`
- Full commit: `35303856599a211fd49aa115cea71037731f9c6c`
- آخرین فاز تکمیل‌شده: Liquid Glass UI
- آخرین PR ادغام‌شده: `#8`

## متن آماده برای چت جدید

> قبل از هر پیشنهاد یا پیاده‌سازی، فایل‌های
> `docs/project/START_HERE.md`،
> `docs/project/AIRBATTERY_MASTER_ROADMAP.md`
> و جدیدترین فایل داخل `docs/project/progress/` را بخوان.
> سپس `git status -sb` و `git log -5 --oneline --decorate` را بررسی کن.
> فقط از مرحله بعدی ثبت‌شده در جدیدترین Snapshot ادامه بده.
