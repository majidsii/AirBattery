# راهنمای تست اصلاح AirPods Pro 2020 روی Ubuntu

این نسخه بر اساس داده واقعی AirPods Pro 2020 با نام `SHABIN` اصلاح شده است. هدف این تست این است که ثابت کنیم درصدهای Left، Right و Case در برنامه واقعاً با بازکردن کیس و خارج‌کردن هر گوشی تغییر می‌کنند.

## چه چیزهایی اصلاح شده است؟

- اسکن خودکار BLE با پنجره ۲ ثانیه‌ای و چرخه ۳ ثانیه‌ای انجام می‌شود؛ بنابراین در صورت انتشار تبلیغ توسط AirPods، تغییرات معمولاً ظرف چند ثانیه به رابط کاربری می‌رسند.
- لایه‌بندی منوی کناری اصلاح شده است تا نام گزینه‌ها همیشه بالاتر از محتوای صفحه نمایش داده شود.
- تبلیغ AirPods با MAC خصوصی و چرخشی می‌تواند به دستگاه جفت‌شده Apple نسبت داده شود.
- بسته‌بودن کیس و قطع‌بودن اتصال صوتی دیگر مانع شناسایی هویت جفت‌شده `SHABIN` نمی‌شود.
- وجود اسپیکر یا هدست Bluetooth دیگر باعث گم‌شدن خودکار تبلیغ AirPods نمی‌شود، به‌شرط اینکه فقط یک دستگاه Apple جفت‌شده معتبر وجود داشته باشد.
- payloadهای واقعی ضبط‌شده به تست‌های parser اضافه شده‌اند.

## ۱. جایگزین‌کردن نسخه قبلی

فایل ZIP جدید را در `Downloads` Extract کن. سپس وارد پوشه پروژه جدید شو. نام پوشه داخل ZIP همان نام قبلی پروژه است؛ بهتر است ابتدا پوشه قدیمی را نگه داری و این نسخه را با نام جدا Extract کنی.

```bash
cd ~/Downloads
unzip AirBattery-AirPods-Pro-2020-fix.zip -d AirBattery-fixed
cd AirBattery-fixed/AirBattery-universal-battery-runtime-2026-07-30
```

## ۲. اجرای اعتبارسنجی و Build کامل

```bash
chmod +x RUN_UBUNTU_VALIDATION.sh scripts/*.sh apps/gnome-extension/scripts/*.sh
./RUN_UBUNTU_VALIDATION.sh
```

این دستور ممکن است برای نصب پیش‌نیازها رمز `sudo` بخواهد. برنامه را با `sudo` اجرا نکن.

در صورت موفقیت، فایل‌های نصب در مسیرهای زیر ساخته می‌شوند:

```text
target/release/bundle/deb/
target/release/bundle/appimage/
```

برای پیدا‌کردن فایل‌ها:

```bash
find target/release/bundle -type f \
  \( -name "*.deb" -o -name "*.AppImage" \) -print
```

## ۳. اجرای نسخه توسعه برای تست سریع

بعد از موفقیت تست‌ها و نصب dependencyها:

```bash
npm --prefix apps/desktop run tauri dev
```

Bluetooth لپ‌تاپ روشن باشد و `SHABIN` قبلاً Pair شده باشد. لازم نیست با درِ بسته متصل بماند؛ این قطع‌شدن طبیعی است.

## ۴. سناریوی تست

1. هر دو گوشی داخل کیس و در بسته.
2. برنامه را باز نگه دار.
3. در کیس را باز کن و حدود ۳ تا ۶ ثانیه صبر کن.
4. فقط گوشی چپ را خارج کن و حدود ۳ تا ۶ ثانیه صبر کن.
5. گوشی راست را هم خارج کن و حدود ۳ تا ۶ ثانیه صبر کن.
6. هر دو را داخل کیس بگذار و در را باز نگه دار.
7. در کیس را ببند.

انتظار می‌رود مقادیر و وضعیت شارژ بدون فشردن Refresh به‌روزرسانی شوند. Case فقط زمانی نمایش داده می‌شود که AirPods آن را در تبلیغ BLE منتشر کند؛ بسته‌شدن کیس یا خارج‌بودن هر دو گوشی ممکن است Case را موقتاً به حالت قدیمی/ناموجود ببرد.

## ۵. اگر Build یا تست شکست خورد

فایل زیر را ارسال کن:

```text
airbattery-validation.log
```

اگر Build موفق بود ولی درصدها تغییر نکردند، این دستور را هم هنگام بازکردن کیس اجرا کن و خروجی‌اش را ارسال کن:

```bash
source "$HOME/.cargo/env"
RUST_LOG=debug cargo run -p airbattery-cli -- scan --seconds 35 --json \
  > airbattery-after-fix.json \
  2> airbattery-after-fix.stderr
```

در این تست، کیس را پنج ثانیه بعد از شروع دستور باز کن.
