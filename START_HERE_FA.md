# شروع تست AirBattery روی Ubuntu 26.04

این نسخه هنوز alpha است و نصب نهایی نیست. ابتدا باید build و تست‌های native روی Ubuntu واقعی اجرا شوند.

## روش ساده

داخل پوشه پروژه روی فایل `RUN_UBUNTU_VALIDATION.sh` راست‌کلیک نکنید؛ یک Terminal در همان پوشه باز کنید و اجرا کنید:

```bash
./RUN_UBUNTU_VALIDATION.sh
```

اسکریپت فقط پیش‌نیازهای build را نصب می‌کند، Rust را در حساب کاربری شما قرار می‌دهد، وابستگی‌ها را دریافت می‌کند و تست‌ها را اجرا می‌کند. دستگاه Bluetooth را unpair نمی‌کند و برنامه را به‌صورت سیستمی نصب نمی‌کند.

در پایان یا هنگام اولین خطا، فایل زیر ساخته می‌شود:

```text
airbattery-validation.log
```

همین فایل را برای ادامه توسعه ارسال کنید.
