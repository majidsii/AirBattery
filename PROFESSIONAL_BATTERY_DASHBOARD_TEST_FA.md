# راهنمای تست نسخه حرفه‌ای AirBattery

این نسخه سه تغییر اصلی دارد:

1. مقدار `255` در packet فعال AirPods به‌عنوان «فعلاً در دسترس نیست» تفسیر می‌شود و دیگر کل گزارش باتری را خراب نمی‌کند.
2. آخرین مقدار معتبر کیس تا زمان انقضای طبیعی داده نگه داشته می‌شود؛ در نتیجه خارج‌کردن هر دو گوشی نباید بلافاصله کارت کیس را حذف کند.
3. صفحه اصلی همه دستگاه‌های Bluetooth متصل را خودکار نمایش می‌دهد و برای خانواده‌های شناخته‌شده از SVG مدل‌محور استفاده می‌کند.

## اعتبارسنجی و Build

```bash
cargo fmt --all

CARGO_INCREMENTAL=0 \
CARGO_PROFILE_DEV_DEBUG=0 \
CARGO_PROFILE_TEST_DEBUG=0 \
CARGO_BUILD_JOBS=2 \
./RUN_UBUNTU_VALIDATION.sh
```

## تست AirPods و کیس

برنامه را با لاگ Debug اجرا کن:

```bash
RUST_LOG='bluetooth_linux=debug,airbattery_desktop=info' \
./target/release/airbattery 2>&1 | tee airbattery-professional-test.log
```

1. در کیس را باز کن و حداقل یکی از گوشی‌ها را چند ثانیه داخل کیس نگه دار تا مقدار کیس دریافت شود.
2. هر دو گوشی را خارج کن.
3. مقدار چپ و راست باید از packet فعال AACP به‌روز شود.
4. اگر کیس مقدار `0xFF` بفرستد، آخرین مقدار معتبر کیس باید باقی بماند و هشدار `percentage 255 is outside 0..=100` نباید دیده شود.
5. وقتی کیس دوباره مقدار معتبر بفرستد، مقدار جدید جایگزین مقدار قبلی می‌شود.

## تست چند دستگاه

1. دو یا چند دستگاه Bluetooth دارای باتری را متصل کن.
2. صفحه Battery را باز کن.
3. همه دستگاه‌های متصل باید بدون انتخاب دستی در کارت‌های جدا نمایش داده شوند.
4. دستگاه ترجیحی فقط ترتیب نمایش را تغییر می‌دهد و دستگاه‌های متصل دیگر را پنهان نمی‌کند.

## تست تصاویر

- مدل‌های شناخته‌شده باید SVG خانواده واقعی خود را نمایش دهند.
- اگر مدل دقیق شناخته نشود، تصویر خانواده یا دسته عمومی نمایش داده می‌شود.
- فایل‌ها محلی، برداری و بدون تصویر PNG/JPEG جاسازی‌شده هستند.
